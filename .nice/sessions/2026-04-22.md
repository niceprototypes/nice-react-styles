# Session 2026-04-22

**User:** Expose the new `BackgroundSizeType` from nice-styles so nice-react-image can consume it.

**Claude (17:57):** Added `BackgroundSizeType` to the explicit `export type { … } from 'nice-styles'` list in `src/index.ts` (alphabetical slot between `BackgroundColorType` and `BorderColorType`). Rebuilt — `dist/index.d.ts` now includes `BackgroundSizeType` in the re-export list.
