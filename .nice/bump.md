[2026-05-18 14:46] patch: StylesProvider and rollup externals updated to follow nice-styles' CSS file rename — import target is now nice-styles/tokens.css (was nice-styles/variables.css). No public-API change.
[2026-05-18 12:00] patch: createTokens trimmed to a thin React shim (310 → 56 lines). The CSS-string-assembly logic now lives in nice-styles as generateTokenCSS. StylesProvider imports parseGoogleFontsUrl/GoogleFontsConfig from nice-styles directly. Nine dead one-line shim files removed from services/ (styleValues, isStyleValue, ModeValue, BreakpointValue, registerTokens, registry/, parseGoogleFontsUrl, getToken, createTokens/tokenStyleSheet). Public surface unchanged.
[2026-05-17 12:00] major: Eliminate getReactToken / getReactTokenKey / getReactTokenValue. The unified getToken from nice-styles now covers every case (registry-aware, runtime-extensible via createTokens). Package shrinks to its React-bridge surface: StylesProvider, FontLoader, withBreakpoints, useBreakpoint, createTokens (React shim). The deprecated getReactToken field on createTokens' return value is also removed. All other prior exports remain reachable via re-exports from nice-styles.
