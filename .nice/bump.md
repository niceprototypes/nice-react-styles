[2026-06-08 00:59] major: Rename createTokens to setTokens
[2026-06-08 23:23] major: Remove StylesProvider loadFonts prop; pass an explicit googleFonts URL instead
[2026-06-09 19:09] patch: StylesProvider builds font links via nice-styles' shared buildGoogleFontsConfig / buildAdobeFontsConfig; link-building deduped out of the provider, behavior unchanged
[2026-06-10 23:59] minor: withBreakpoints precedence is now specificity-first via nice-styles shared logic — at a viewport, all matching keys sort least-to-most specific and merge so the most specific wins (was: ranges by insertion order then exact last). Same object API; tie-break behavior refined. BreakpointKey type now re-exported from nice-styles
[2026-06-15 22:38] minor: Re-export getHSLA (and GetHSLAOptions) from nice-styles so React consumers can import the HSLA-token adjuster from nice-react-styles
[2026-06-17 21:23] minor: StylesProvider accepts a links prop (LinkAttributes[]) for self-hosted / custom font stylesheets, injected via FontLoader alongside googleFonts/adobeFonts.
[2026-06-21 16:46] minor: StylesProvider accepts a detectDevice prop — when set, it runs device detection (nice-react-device-detector) and publishes it on context; read the shared { isMobile } with the new useDevice hook (also re-exported). Defaults off, so useDevice returns { isMobile: false } until opted in.
[2026-06-21 17:12] minor: StylesProvider accepts a detectTheme prop — sibling of detectDevice. When set, it detects the OS color-scheme preference and publishes it on context; read the shared { isNight } with the new useTheme hook. Defaults off, so useTheme returns { isNight: false } until opted in.
[2026-06-22 00:00] minor: useTheme() now returns { theme } (a ThemeType name: "day" | "night" | custom) instead of { isNight }, so additional themes can be added without a breaking signature change. Branch on theme === "night" rather than an isNight flag. Detection-off / no-provider default is { theme: DEFAULT_THEME }.
[2026-06-27 16:50] minor: Internalize mobile detection — fold the former nice-react-device-detector logic into StylesProvider (new internal useDeviceDetection hook) and drop the nice-react-device-detector dependency. Public surface unchanged: device + theme are read via useDevice()/useTheme(); useDeviceDetector is no longer needed or re-exported. The standalone nice-react-device-detector package is being phased out.
[2026-07-01 17:27] minor: useDevice(mobileUserAgents?) now returns { userAgent, mobileUserAgents, isMobile } instead of { isMobile }, and accepts a mobileUserAgents list to override the default MOBILE_USER_AGENTS (now exported) per call. isMobile is true when userAgent matches that list OR the existing touch / small-screen / debug signals fire, so existing { isMobile } consumers are unaffected. The DeviceContextValue type is renamed to DeviceState.
[2026-07-01 18:56] patch: Fix FontLoaderProps type resolution — import LinkAttributes from nice-styles (where it is defined) instead of the local ../../types barrel, which never re-exported it. Type-only fix; emitted output unchanged. Also add an ambient "*.css" module declaration so the StylesProvider `import "nice-styles/tokens.css"` side-effect import type-checks.
